diff -Nur -x '*.orig' -x '*.rej' smeserver-spamassassin-1.3.0/root/etc/e-smith/db/configuration/migrate/10spamassassin mezzanine_patched_smeserver-spamassassin-1.3.0/root/etc/e-smith/db/configuration/migrate/10spamassassin
--- smeserver-spamassassin-1.3.0/root/etc/e-smith/db/configuration/migrate/10spamassassin	2005-09-24 07:25:26.000000000 +1000
+++ mezzanine_patched_smeserver-spamassassin-1.3.0/root/etc/e-smith/db/configuration/migrate/10spamassassin	2005-10-10 11:18:30.450601269 +1000
@@ -17,6 +17,18 @@
 	$DB->set_prop('spamassassin', $old2new{$key}, $value);
     }
 
+    for my $value (values %old2new)
+    {
+	next if ($value eq 'TagLevel');
+
+	my $prop = $DB->get_prop('spamassassin', $value);
+
+	next unless ($prop eq "0" or $prop eq "1");
+
+	$DB->set_prop('spamassassin', $value, 
+		$prop eq "1" ? "enabled" : "disabled");
+    }
+
     my $value = $DB->get_prop_and_delete('spamassassin', 'rewrite_subject');
 
     if (defined $value)
